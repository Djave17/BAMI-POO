/**
 * Cancelar parada
 *
 * Propósito: Reservar el caso de uso que manejará la cancelación operativa de una parada.
 *
 * Capa: domain.
 *
 * Responsabilidades:
 * 
 *   - Aplicar reglas antes de marcar una parada como cancelada.
 *   - Delegar la persistencia del cambio al repositorio.
 *   - No acceder directo a infraestructura; este archivo es un placeholder o base inicial.
 * 
 */
package com.example.recolectores.feature.paradas.domain.usecase
