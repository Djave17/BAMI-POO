/**
 * ViewModel de parada completada
 *
 * Propósito: Reservar el coordinador de presentación para el cierre final de una parada.
 *
 * Capa: presentation.
 *
 * Responsabilidades:
 * 
 *   - Recibir `UiEvent`, pedir el resumen final y publicar `UiState`.
 *   - Coordinar el siguiente paso de la jornada después del cierre.
 *   - No acceder directo a API, Room, DataStore o WebSocket; este archivo es un placeholder o base inicial.
 * 
 */
package com.example.recolectores.feature.paradas.presentation.completed
