/**
 * ViewModel de espera del donante
 *
 * Propósito: Reservar el coordinador de presentación para el estado de espera de una parada.
 *
 * Capa: presentation.
 *
 * Responsabilidades:
 * 
 *   - Procesar `UiEvent`, invocar casos de uso y publicar `UiState`.
 *   - Coordinar extensión o cierre de espera sin exponer detalles técnicos a la UI.
 *   - No acceder directo a API, Room, DataStore o WebSocket; este archivo es un placeholder o base inicial.
 * 
 */
package com.example.recolectores.feature.paradas.presentation.waiting
