/**
 * Implementación de autenticación
 *
 * Propósito: Reservar la implementación de datos que conectará el dominio de autenticación con fuentes locales y remotas.
 *
 * Capa: data.
 *
 * Responsabilidades:
 * 
 *   - Coordinar llamadas a API, persistencia de sesión y lectura local cuando se implemente.
 *   - Traducir datos técnicos a modelos estables para la capa domain.
 *   - No ser usado directo por la UI; este archivo es un placeholder o base inicial.
 * 
 *
 * Nota: Este repositorio debe conectar dominio con datos locales o remotos. No debe ser consumido desde Composables ni ViewModels sin pasar por casos de uso.
 */
package com.example.recolectores.feature.autenticacion.data.repository
